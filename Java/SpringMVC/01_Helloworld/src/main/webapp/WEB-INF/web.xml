<!DOCTYPE web-app PUBLIC
 "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
 "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app>
  <display-name>Archetype Created Web Application</display-name>

  <!--配置SpringMVC的前端控制器DispatcherServlet接收和处理所以请求-->
  <servlet>
    <servlet-name>SpringMVC</servlet-name>
    <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    <!--配置SpringMVC的配置文件-->
    <init-param>
      <!--固定值-->
      <param-name>contextConfigLocation</param-name>
      <!--类路径下,包括java目录和resources目录-->
      <param-value>classpath:springmvc.xml</param-value>
    </init-param>

    <!--使DispatcherServlert前端控制器在服务器启动时就创建.
    because:Servlet默认都会在第一次请求访问时被创建,而前端控制器为框架的核心组件,有太多初始化操作要做,如
    加载SpringMVC的配置文件等.因此将它的创建提前到服务器启动,避免第一次请求访问过慢
    -->
    <load-on-startup>1</load-on-startup>

  </servlet>
  <servlet-mapping>
    <servlet-name>SpringMVC</servlet-name>
    <!--/被服务器解析为工程路径
      /表示接收除了.jsp之外的所有请求
      /*表示接收所以请求
    -->
    <url-pattern>/</url-pattern>
  </servlet-mapping>

</web-app>

