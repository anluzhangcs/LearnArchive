<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
            http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd
            http://www.springframework.org/schema/aop https://www.springframework.org/schema/aop/spring-aop.xsd">

    <context:component-scan base-package="org.zhang"/>

    <context:property-placeholder location="classpath:jdbc.properties"/>

    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
        <property name="driverClassName" value="${prop.driverClassName}"/>
        <property name="url" value="${prop.url}"/>
        <property name="username" value="${prop.username}"/>
        <property name="password" value="${prop.password}"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!--创建事务管理器-->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <!--注入数据源,表示事务管理用于哪个数据库-->
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!--通过AOP实现事务管理-->
    <!--1.配置通知-->
    <tx:advice id="txadvice" transaction-manager="transactionManager">
        <!--设置通知属性-->
        <tx:attributes>
            <!--设置对哪些方法添加事务,可以模糊匹配-->
            <tx:method name="transferAccount"/>
            <!--<tx:method name="transfer*"/>,表示以transfer开头 -->
        </tx:attributes>
    </tx:advice>

    <!--2.配置切入点和切面-->
    <aop:config>
        <!--切入点-->
        <aop:pointcut id="pointcut" expression="execution(* org.zhang.service.BankService.transferAccount(..))"/>
        <!--切面.普通AOP使用aspect,事务切面使用advisor-->
        <aop:advisor advice-ref="txadvice" pointcut-ref="pointcut"/>
    </aop:config>


</beans>